<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Facpya_Proyecto.view.Recuperar"
             BackgroundColor="#9D0924">
    <ContentPage.Content>
        <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">

            <StackLayout Orientation="Horizontal" 
                         HorizontalOptions="Center" 
                         VerticalOptions="Start">
                <Image Source="/drawable/Logo.jpg" 
                       Grid.Row="0" 
                       Aspect="AspectFit" 
                       Margin="0,10,0,0"/>
                <StackLayout VerticalOptions="Center">
                    <Label Text="ÁREAS" 
                           TextColor="White" 
                           FontSize="50" 
                           FontAttributes="Bold" 
                           FontFamily="Arial"></Label>
                    <Label Text="FACPYA" TextColor="White" 
                           FontSize="50" FontAttributes="Bold" 
                           FontFamily="Arial" 
                           Margin="30,-25,0,0"></Label>
                </StackLayout>
            </StackLayout>

            <Frame Padding="20"
                   CornerRadius="10"
                   BackgroundColor="White"
                   VerticalOptions="CenterAndExpand"
                   HorizontalOptions="FillAndExpand"
                   Margin="20,-150,20,0">

                <StackLayout>

                    <Label Text="Recuperar contraseña"
                           FontSize="Large"
                           HorizontalOptions="Center"
                           Margin="0,0,0,20" />

                    <Entry Placeholder="Correo registrado" x:Name="TxtEmailRegistrado" />

                    <Button Text="Recuperar"
                            TextColor="White"
                            HorizontalOptions="FillAndExpand"
                            BackgroundColor="#3C252D"
                            CornerRadius="20"
                            Margin="70,0,70,0"
                            Clicked="Button_Clicked_1"
                            />
                </StackLayout>
            </Frame>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>